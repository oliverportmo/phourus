// Generated by CoffeeScript 1.6.2
(function() {
  define(["jquery", "underscore", "backbone", "text!html/orgs/admin.html"], function($, _, Backbone, template) {
    var view;

    view = Backbone.View.extend({
      initialize: function() {},
      render: function() {
        var compiled, data;

        data = {};
        Backbone.Events.trigger("sidebar", "admin");
        compiled = _.template(template, data);
        this.$el.html(compiled);
        return compiled;
      }
    });
    return view;
  });

}).call(this);
