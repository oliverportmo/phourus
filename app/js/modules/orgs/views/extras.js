// Generated by CoffeeScript 1.6.2
define(["jquery", "underscore", "backbone", "text!html/orgs/extras.html"], function($, _, Backbone, template) {
  var view;

  view = Backbone.View.extend({
    initialize: function() {
      return this.render();
    },
    render: function() {
      var compiled, data;

      data = {};
      Backbone.Events.trigger("sidebar", "default");
      compiled = _.template(template, data);
      this.$el.html(compiled);
      return compiled;
    }
  });
  return view;
});
